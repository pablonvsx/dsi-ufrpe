// Barrel de contextos.
// Futuramente: exportar aqui os contextos globais do app (ex.: `AuthContext`).
// contexts/index.ts
export { AuthProvider, useAuth } from "./auth-context";
// Quando tiver mais contextos (tema, notificações etc.), adiciona aqui também:
// export { ThemeProvider, useTheme } from "./ThemeContext";